import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.quanlychitieu',
  appName: 'QuanLyChiTieu',
  webDir: 'dist'
};

export default config;
